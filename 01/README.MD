1) EDA:
    1) Выявлены признаки с наибольшим коэффициентом корреялции 
    2) Добавлены новые признаки на основе графиков зависимостей целевой переменной и признаков. Например year*year - Новый признак.
    3) Так же преобразован признак km_driven в вид 1/km_driven - на основании графика зависимости selling_price и km_driven

2) Генерация новых фичей и доп преобразование фичей: 
    1) Была созданы две новые фичи torque_nm, torque_rpm
    2) Вытащены названия марок автомобилей - предположу, что лучше строить лин регрессии для каждой марки автомобиля свои
    3) Прологарифирован таргет - это позволило привести распределение таргета к нормальному виду что существенно сказалось на качестве модели 
    4) Не удалось очистить выборку от выбросов, хотя логарифмирование частично это решает, но можно лучше подготовить данные 
    5) Применен OHE к категориальным признакам - существенно увеличило кол-во признаков модели за счет колонки marka 

3) Построение модели 
    1) Были построены разные модели на стандатизированных данных и нет, с разными видами регуляризации - в результате лучшая модель  Ridge регрессия на стандатизированных данных (числовые) c логарифмированным таргетом. 
    2) Качество модели
        Train: r2 0.8846018911793789, mse: 0.06743691231643355 (Логарифмированны таргет)
        Test: r2 0.8193840614281576,  mse: 0.12919587395225982 (Логарифмированны таргет)
        gs_ridge_num_cat_std | Доля прогнозов с ошибкой менее 10%: 99.4 %


4) Сохранена модель в виде pkl файла с весами в папке models

5) Не удалось сделать приложение StreamLit:
    Основная проблема - тайм менеджмент

6) Из улучшений:
    1) попробовал разрабоать разные модели под разные марки, либо объединить их на виды: премиум, средний, эконом (условно) и для каждой категории построить модель. Возможно, что такой подход принесет лучшее качество метрики 
    2) Доделать streamLit - фактически все упирается в написание кода EDA pipeLine.